apply plugin: 'groovy'

repositories {
    mavenCentral()
}

dependencies {
    compile localGroovy() //TODO maybe should use specified groovy version instead?
    compile 'com.amazonaws:aws-java-sdk:1.6.0'
}

libsDirName = '../../lib'